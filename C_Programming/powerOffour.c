#include<stdio.h>

int main()
{
    int num;
    scanf("%d", &num);
    // 100, 10000, 1, 1000000
    // 10100  (1<<5) 
    // 10000
    one=0, zero=0
    while n>0
        if (n>>1 & 1) ==1:
            one++;
        zero++;
    if(one == 1 && (float)zero/2 ==0.0)
        // multiple of 4

}
